version: '3.8'

services:
  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_DATABASE=realtime
    networks:
      - rtspstreamer_network  

  seeder:
    build:
      context: ./video-streaming-backend
    container_name: seeder
    depends_on:
      - mongo
    environment:
      - MONGODB_URI=mongodb://mongo:27017/realtime
      - RTSP_URL=rtsp://admin:*Simm12345@10.10.90.100/Streaming/Channels/1
    command: ["node", "seeder.js"]
    networks:
      - rtspstreamer_network

  backend:
    build:
      context: ./video-streaming-backend
    container_name: backend
    depends_on:
      - mongo
    environment:
      - MONGODB_URI=mongodb://mongo:27017/realtime 
      - RTSP_URL=rtsp://admin:*Simm12345@10.10.90.100/Streaming/Channels/1
    ports:
      - "3000:3000"
    networks:
      - rtspstreamer_network  

  frontend:
    build:
      context: ./video-streaming-frontend
    container_name: frontend
    ports:
      - "5000:3000"
    depends_on:
      - backend
    networks:
      - rtspstreamer_network

networks:
  rtspstreamer_network:
    name: rtspstreamer_network
    driver: bridge